<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqupt.mapper.PapertagMapper">
    <!--根据分类名称来查询-->
    <select id="savePapertag" resultType="com.cqupt.domin.Papertag">
        insert into papertag(tagid,paperid) values (#{tagid},#{paperid});
    </select>
</mapper>
